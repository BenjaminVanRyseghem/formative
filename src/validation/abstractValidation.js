/**
 * TODO: Write jsdoc
 */
export default class AbstractValidation {
	isRequired({ id: _, stateHandler: __, state: ___, validator: ____ }) {}

	makeValidateFor({
		id: _,
		value: __,
		stateHandler: ___,
		state: ____,
		validator: _____,
		form: ______
	}) {}

	validate({ state: _, stateHandler: __, validator: ___, form: ____ }) {}

	convertJsonState({ state: _ }) {}

	convertGetterSetterState({ state: _, context: __ }) {}
}
